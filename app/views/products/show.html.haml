#main.ten.columns
  %h2= @product.code

  %article
    %header
      %h3= t 'label.record_data'
      = link_to t('label.edit'), edit_product_path(@product), :class => 'button-pill'

    %table.product
      %tr
        %th= t 'simple_form.labels.company'
        %td= link_to @product.company.name, @product.company
