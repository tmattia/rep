- title "#{t('label.product.edit')} #{@product.code}"
- breadcrumbs.add @product.code, product_path(@product), :i18n => false
- breadcrumbs.add 'label.product.edit'

#main.ten.columns
  %h2= t 'label.product.edit'

  = render 'form'

