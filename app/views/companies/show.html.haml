#main.ten.columns
  %h2= @company.name

  %article
    %header
      %h3= t 'label.record_data'
      = link_to t('label.edit'), edit_company_path(@company), :class => 'button-pill'

    %table.company
      %tr
        %th= t 'label.email'
        %td= @company.email
